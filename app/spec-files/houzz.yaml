# This is an example of the manually-written Houzz API
# Example: https://www.houzztest.com/api?format=xml&version=140&siteId=101&app=test1&user=&token=&appAgent=iPhone5,2~iOS%25208.2~com.houzz.app~Build%25201477&method=getDigest&thumbSize1=2&getGalleryTopic=yes
swagger: '2.0'
info:
  title: Houzz API
  description: Houzz
  #version: '1.0.0'
  version: '54'
# the domain of the service
host: www.houzztest.com
# array of all schemes that your API supports
schemes:
  - https
# will be prefixed to all paths
#basePath: /v1
produces:
  - application/xml
  #- application/json
paths:
  /api?method=getDigest:
    post:
      x-minimumVersion: 53
      summary: Digest of latest galleries, questions, and their cover topics.
      description: Get a summary recent updates from Houzz (currently for mobile devices).
      parameters:
        - name: level
          in: query
          description: |
            A bit mask to select what to get.
            * 1: Get cover topics for galleries.
            * 2: Get latest galleries.
            * 4: Get cover topics for questions.
            * 8: Get latest questions.
          type: number
          format: integer
        - name: thumbSize1
          in: query
          description: Thumbnail size ID for images in galleries or questions.
          type: number
          format: integer
        - name: numberOfGalleries
          in: query
          description: Maximum number of galleries to get. Only when the level parameter contains the Gallery bit.
          type: number
          format: integer
        - name: numberOfQuestions
          in: query
          description: Maximum number of questions to get. Only when the level parameter contains the Question bit.
          type: number
          format: integer
      tags:
        - CoverTopics
      responses:
        200:
          description: Galleries and questions.
          schema:
            type: object
            properties:
              Galleries:
                type: array
                items:
                  $ref: '#/definitions/Gallery'
                description: List of galleries. Only when the level parameter contains the Gallery bit.
              Questions:
                type: array
                items:
                  $ref: '#/definitions/Question'
                description: List of questions. Only when the level parameter contains the Question bit.
              GalleryCoverTopics:
                type: array
                items:
                  $ref: '#/definitions/CoverTopic'
                description: List of cover topics for galleries. Only when the level parameter contains the GalleryCoverTopic bit.
              QuestionCoverTopics:
                type: array
                items:
                  $ref: '#/definitions/CoverTopic'
                description: List of cover topics for questions. Only when the level parameter contains the QuestionCoverTopic bit.
        default:
          description: Unexpected error
definitions:
  Image:
    properties:
      ThumbUrl1:
        type: string
        format: URL
        description: Thumbnail image URL.
      ThumbWidth1:
        type: number
        format: integer
        description: Thumbnail image width.
      ThumbHeight1:
        type: number
        format: integer
        description: Thumbnail image height.
      OptionalThumbSizes:
        type: array
        description: Thumbnail image optional size IDs.
        items:
          properties:
            ThumbSizeId:
              type: number
              format: integer
              description: Thumbnail size ID.
  CoverTopic:
    x-minimumVersion: 79
    properties:
      Id:
        type: number
        format: integer
        description:  Topic ID.
      Name:
        type: string
        description: Topic title.
      CoverImages:
        type: array
        items:
           $ref: '#/definitions/Image'
  Gallery:
    properties:
      Id:
        type: number
        format: integer
        description: Gallery ID, such as 12345.
      Title:
        type: string
        description: Gallery title, such as "Houzz Tour".
      Subtitle:
        type: string
        description: Gallery subtitle.
      Description:
        type: string
        description: Gallery subtitle.
      IsPrivate:
        type: boolean
        description: Whether the gallery is only visible to its creator.
      CreateBy:
        type: string
        description: Creation user's profile name.
      CreateByName:
        type: string
        description: Creation user's full name.
      CreateByImage:
        type: string
        format: URL
        description: Creation user's avatar image URL.
      Modified:
        type: string
        format: date-time
        description: Modification date and time, such as "2014-03-10T04:03:40.00Z".
      ModifiedAsString:
        type: string
        description: Modification date, such as "March 9, 2014".
      Featured:
        type: string
        format: date-time
        description: Date and time when the gallery get featured.
      SpaceCount:
        type: number
        format: integer
        description: Total number of spaces in the gallery.
      CommentCount:
        type: number
        format: integer
        description: Total number of comments received.
      Likes:
        type: number
        format: integer
        description: Total number of likes received.
      ThemeType:
        type: string
        enum:
          - regular
        description: Gallery theme.
      SpaceImages:
        type: array
        items:
           $ref: '#/definitions/Image'
  QuestionStatus:
    type: string
    enum:
      - open
      - resolved
      - locked
      - buried
      - deleted
    description: Gallery theme.
  Question:
    properties:
      QuestionId:
        type: number
        format: integer
        description: Question ID, such as 12345.
      Status:
         $ref: '#/definitions/QuestionStatus'
      Title:
        type: string
        description: Question title.
      Body:
        type: string
        description: Question content.
      Topic:
        type: number
        format: integer
        description: Topic ID.
      Likes:
        type: number
        format: integer
        description: Total number of likes received.
      CreatedDate:
        type: string
        format: date-time
        description: Creation date and time, such as "2014-03-10T04:03:40.00Z".
      CreateBy:
        type: string
        description: Creation user's profile name.
      CreateByName:
        type: string
        description: Creation user's full name.
      CreateByImage:
        type: string
        format: URL
        description: Creation user's avatar image URL.
      IsPool:
        type: boolean
        description: If the question is a poll.
      TotalVoteCount:
        type: number
        format: integer
        description: Total number of votes received if it is a poll.
      ReplyCount:
        type: number
        format: integer
        description: Total number of answers received.
      HasImage:
        type: boolean
        description: If it contains image.
      FileImages:
        type: array
        items:
           $ref: '#/definitions/Image'
